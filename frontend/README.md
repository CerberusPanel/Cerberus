# Cerberus Frontend

## Local Network Access

The Vite dev server listens on all network interfaces, so devices on the same network can open the frontend with your machine IP:

```text
http://192.168.1.214:5173
```

Start the dev server normally:

```bash
npm run dev
```

HTTPS is optional and can be configured separately by the user if needed.
